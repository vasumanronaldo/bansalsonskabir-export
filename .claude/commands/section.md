---
description: Build one page section end-to-end
---
Argument: the page and section, e.g. `/craftsmanship — The sequence`.

1. Read ONLY that section's entry in `docs/04-pages.md` and the relevant tokens
   in `docs/02-design-system.md`. Do not read whole docs.
2. State the section's job in one sentence and which of the five proofs it advances.
3. Build it as a self-contained component in `components/blocks/`.
4. Use approved copy verbatim. Mark any gap with a COPY comment.
5. Check: 360px, keyboard focus, reduced motion, no more than 3 gold elements.
